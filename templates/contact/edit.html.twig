{% extends 'base.html.twig' %}

{% block title %}Edit Contact{% endblock %}

{% block body %}

{% if is_granted('ROLE_ADMIN') %}

    <h2 class="subtitle">Modifier le contact</h2>

    {{ include('contact/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('app_contact_index') }}" class="btn btn-warning">Retour aux contacts</a>

    {{ include('contact/_delete_form.html.twig') }}

{% else %}
    <h2 class="subtitle">Vous n'avez pas les droits pour modifier ce contact</h2>
{% endif %}
{% endblock %}
