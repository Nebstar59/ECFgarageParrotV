{% extends 'base.html.twig' %}

{% block title %}Photo{% endblock %}

{% block body %}
    <h2 class="subtitle"> {{ photo.title }} </h2>
    <img src="{{ asset('images/cars/' ~ photo.imageName) }}" alt="{{ photo.title }}" class="img-fluid">

    <a href="{{ path('app_car_index') }}" class="btn btn-warning">Retour aux véhicules</a>
{% if is_granted('ROLE_ADMIN') %}
    <a href="{{ path('app_photo_edit', {'id': photo.id}) }}" class="btn btn-warning">Modifier</a>
    {{ include('photo/_delete_form.html.twig') }}
{% endif %}
{% endblock %}
