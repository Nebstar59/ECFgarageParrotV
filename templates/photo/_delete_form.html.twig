<form method="post" action="{{ path('app_photo_delete', {'id': photo.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cette photo?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ photo.id) }}">
    <button class="btn btn-danger">Supprimer la photo</button>
</form>
