{% extends 'base.html.twig' %}

{% block title %}Edit Car{% endblock %}

{% block body %}

{% if is_granted('ROLE_ADMIN') %}
    <h1>Modifier l'annonce de la voiture</h1>

    {{ include('car/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('app_car_add_photo', {'id': car.id}) }}">Ajouter une photo à l'annonce</a>
    <a href="{{ path('app_car_index') }}">Retour à la liste des annonces</a>

    {{ include('car/_delete_form.html.twig') }}
{% else %}
    <h1>Vous n'avez pas les droits pour modifier cette annonce</h1>
    <a href="{{ path('app_car_index') }}">Retour à la liste des annonces</a>
{% endif %}
    
{% endblock %}
